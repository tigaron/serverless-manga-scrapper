service: manga-scrapper-project
frameworkVersion: '3'

provider:
  name: aws
  stage: production
  region: ap-southeast-1
  runtime: nodejs16.x
  memorySize: 512
  timeout: 30

functions:
  api:
    handler: handler.handler
    events:
      - httpApi: '*'
