#!/usr/bin/env bash
# yarn workspace root run prod &&
yarn workspace scraper run prod &&
yarn workspace cron run prod #&&
# yarn workspace series run prod &&
# yarn workspace search run prod &&
# yarn workspace status run prod &&
# yarn workspace chapters run prod
